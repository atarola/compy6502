`timescale 1ns / 1ps

/**
 * Spec: byte_engine chip-select decode.
 *
 * Verify that each cs_select value asserts exactly one active-low SPI chip
 * select during a transfer and leaves the other chip-select lines inactive.
 */
module byte_engine_chip_select_tb;
endmodule
