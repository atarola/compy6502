[project]
name = "compy6502"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "py65==1.1.0",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.pytest.ini_options]
testpaths = ["test"]
pythonpath = ["test"]

[tool.ruff]
line-length = 120
target-version = "py311"
